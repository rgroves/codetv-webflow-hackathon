---
import "./MapNode.css";

const { classMods, nodeType, ariaLabel } = Astro.props;
---

<button
  class={`node ${classMods}`}
  data-node={nodeType}
  type="button"
  aria-label={ariaLabel}></button>
